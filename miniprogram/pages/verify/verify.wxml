<scroll-view class="page" scroll-y="true">
    <t-input align="right" size="small" value="{{spr}}" data-inputfield="spr" onchange="setInputData" label="slot" placeholder="请输入审批人">
        <text slot="label" style="font-size: 28rpx;">审批负责人<text style="color: #e34d59"> *</text> </text>
        <view slot="suffix" style="font-weight: bold;">
            <view style="color: red;" wx:if="{{!sprDrawOk}}">
                ×
            </view>
            <view style="color: green;" wx:if="{{sprDrawOk}}">
                <t-icon name="check" size="18" data-name="check" />
            </view>
        </view>
    </t-input>
    <view style="display: flex;justify-content: center;align-items: center;">
        <canvas style="width: 100%;height: 250px;display:{{sprDrawShow?'block':'none'}};" id="sprSign" class="sprSign" data-name="spr" type="2d" disable-scroll="true" bindtouchstart="touchstart" bindtouchmove="touchmove" canvas-id="sprSign"></canvas>
    </view>
    <!-- {{sprDrawShow}} -->
    <view wx:if="{{sprDrawShow}}" style="display: flex;justify-content: space-between;padding: 0 20rpx;padding-bottom: 30rpx;">
        <t-button t-class="external-class" data-name="spr" theme="danger" variant="outline" block size="small" bindtap="resign">
            重新签字
        </t-button>
        <t-button t-class="external-class" data-name="spr" theme="primary" variant="outline" block size="small" bindtap="signOk">
            完成签字
        </t-button>
    </view>
    <image wx:if="{{sprBase64}}" style="margin:0;width:100%" src="data:image/jpeg;base64,{{sprBase64}}" />
    <view>
        <t-button wx:if="{{!sprDrawShow}}" t-class="external-class" data-name="spr" theme="primary" variant="outline" block size="small" bindtap="openSign">
            {{!sprDrawOk?"开始签字":"重新签字"}}
        </t-button>
    </view>
    <view>
        <t-textarea align="right" data-inputfield="sprbz" onchange="setInputData" autosize value="{{sprbz}}" label="审批负责人意见" placeholder="请输入审批负责人意见" />
    </view>
    <view style="display: flex;justify-content: space-between;margin-top: 20rpx;">
        <t-button t-class="external-class" style="width: 300rpx;" theme="danger" bindtap="refuse">拒绝</t-button>
        <t-button t-class="external-class" style="width: 300rpx;" theme="primary" bindtap="accept">同意</t-button>
    </view>
    <t-toast id="toast" />
</scroll-view>
<t-dialog visible="{{showDialog}}" title="此申请已经被{{info.spr}}审批" confirm-btn="回到首页" bind:confirm="goHome" />